apiVersion: scribe/v1
kind: EdgeTypeDefinition
metadata:
  name: cites
spec:
  whenToUse: |
    Use cites when a note or concept draws from a source artifact.
  semantics: |
    Knowledge provenance. Tracked by wikilinks. No structural enforcement.
