apiVersion: scribe/v1
kind: EdgeTypeDefinition
metadata:
  name: documents
spec:
  whenToUse: |
    Use documents when a doc or ref artifact describes another artifact.
  semantics: |
    Documentation link. Surfaced in briefing. No structural enforcement.
