apiVersion: scribe/v1
kind: Label
metadata:
  name: kind.support.paragraph
spec:
  prefix: PAR
  code: PAR
  children: []
  lifecycle:
    defaultStatus: work.active
  whenToCreate: |
    Paragraph artifacts are sub-section units. Not used in current Scribe
    surface — reserved for future fine-grained document structure.
  agentNote: |
    Internal kind. Do not create paragraph artifacts directly.
