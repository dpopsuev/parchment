name: template_conformance_proposed
trigger: status_changed
when: to=decision.proposed
action: block
forceable: true
check: template_conformance_promote
message: "artifact does not conform to template — required sections missing at promotion to proposed"
